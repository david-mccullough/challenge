{
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^4.1.1",
    "ws": "^8.2.2"
  },
  "scripts": {
    "start": "cd backend/src/ && node index.js",
    "deploy": "git subtree push heroku main",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend/src && node index.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}
